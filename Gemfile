source 'https://rubygems.org'

ruby '3.3.5'

gem 'rails', '~> 7.1.0'
gem 'sqlite3', '~> 1.4'
gem 'puma', '~> 6.0'
gem 'webrick'
gem 'ostruct'
gem 'jbuilder', '~> 2.0'
gem 'turbolinks', '~> 5'
gem 'jquery-rails'

group :development, :test do
  gem 'byebug'
  gem 'pry'
  gem 'rspec-rails', '~> 6.0'
end

group :development do
  gem 'spring'
end

# Asset pipeline gems (optional, for legacy support)
# gem 'sassc-rails'
# gem 'coffee-rails', '~> 5.0'

# Remove deprecated gems: sass-rails, uglifier
# Use modern JS/CSS bundling if needed (jsbundling-rails, cssbundling-rails)
